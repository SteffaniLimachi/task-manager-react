# Frontend (Vite) — se usa como build arg al construir la imagen del frontend
VITE_API_URL=http://localhost:4000

# Backend (usado por docker-compose para levantar el servicio backend)
DATABASE_URL=postgresql://appuser:apppass@postgres:5432/appdb
PORT=4000
FRONTEND_URL=http://localhost:5173
